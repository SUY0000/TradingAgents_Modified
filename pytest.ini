[pytest]
testpaths = tests
python_files = test_*.py
python_classes = *Test
python_functions = test_*
addopts = 
    --strict-markers
    -ra
    -q
markers =
    unit: unit test
    integration: integration test
    compatibility: compatibility test
    performance: performance test
    slow: slow test
    network: test requires network
